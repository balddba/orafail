[project]
name = "live-failure"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "oracledb>=3.4.2",
    "pydantic>=2.13.1",
    "pyyaml>=6.0.3",
    "rich>=14.3.3",
]
